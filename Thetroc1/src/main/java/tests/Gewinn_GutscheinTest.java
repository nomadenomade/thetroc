package tests;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

import DAO.userDAO;

public class Gewinn_GutscheinTest {
/*	
@Test
void getRatekaufer() {
	userDAO dao = new userDAO();
	assertEquals(0.5, dao.getRate("kaufer"));
}

@Test
void getGewinnkaufer() {
	userDAO dao = new userDAO();
	assertEquals(0.1, dao.gewinnBerechnung(3, 20));
}

@Test
void getListbetreiber() {
	userDAO dao = new userDAO();
	assertEquals(0, dao.getListBetreiber().size());
}
*/
@Test
void thetrocpay() {
	userDAO dao = new userDAO();
	//assertEquals(0, dao.thetrocPay(20, "10", 1, "5", "nomade", "nomade@gmail.com", "banane",3));
}
}
